# NoteCloud Sync 用户使用指南

> 适用于 NoteCloud Sync 0.4.2。网站：[notecloud.asia](https://notecloud.asia)。测试阶段通过 BRAT 安装，插件仓库：`lby7469/obsidianplug`。先完成安装与首次同步，再阅读下方隐私说明。

## 一、安装 Obsidian 与 BRAT

1. 安装并打开 Obsidian。
2. 打开“设置 → 第三方插件”，按 Obsidian 的提示启用第三方插件。
3. 点击“浏览”，搜索并安装 **BRAT**，然后启用 BRAT。
4. 打开“设置 → BRAT”，选择“添加 Beta 插件”。
5. 输入仓库地址 `lby7469/obsidianplug`（也可粘贴完整 GitHub 地址）。
6. 选择跟随最新版本并确认安装。
7. 回到“设置 → 第三方插件”，确认 **NoteCloud Sync** 已启用。

安装按钮名称可能随语言和 BRAT 版本不同而变化。可参考 [BRAT 官方说明](https://github.com/TfTHacker/obsidian42-brat) 和 [Obsidian 第三方插件说明](https://help.obsidian.md/Extending+Obsidian/Community+plugins)。第三方插件可以访问本地仓库，请只安装信任的来源。

## 二、登录、注册与忘记密码

1. 打开“设置 → NoteCloud Sync”。
2. 在“账户”区点击“登录”“注册”或“忘记密码”。
3. 浏览器会打开 NoteCloud 网站；密码和验证码只在网站填写。
4. 登录或注册成功后，在授权页确认连接当前 Obsidian 设备。
5. 浏览器通过 `obsidian://notecloud-auth` 一次性回调 Obsidian。

插件只保存可撤销、可轮换的设备令牌，不保存登录密码。如果浏览器没有自动返回 Obsidian，请允许系统打开 `obsidian://` 链接，再从插件重新发起一次登录。

## 三、第一次同步

1. 先确认要同步“所有文件夹”，还是只选择指定文件夹。
2. 默认自动同步间隔为 5 分钟，也可点击状态栏的 NoteCloud 图标立即同步。
3. 第一次连接已有云端仓库时，建议先备份本地 Vault。
4. 同步完成后，设置页顶部会显示已使用空间与总额度。

支持 Markdown、Canvas、PDF、常见图片、音视频和 Office 附件。文件夹层级和路径会保留。Markdown 的 Wiki Link、嵌入目标、标题/块片段和别名会从 Obsidian 元数据中解析并上传，供网站阅读器跳转。

## 四、文件冲突如何处理

插件为每个已同步文件记录：

- 上次本地内容的 SHA-256；
- 上次云端 revision；
- Markdown 上次同步文本（作为三方合并基线）。

同步时，如果本地和云端只有一方变化，直接采用变化的一方。如果两边都变化：

1. Markdown 先进行按行三方合并。
2. 修改区域互不重叠时自动合并，并把合并结果上传。
3. 修改区域重叠，或文件是图片/PDF等二进制文件时，按设置中的冲突策略处理。

默认策略为“保留两份”：本地版本保存成类似 `笔记.conflict-2026-09-04T...md` 的副本，原路径显示云端版本。也可选择“优先本地”或“优先云端”；后两种策略可能覆盖另一端内容，使用前应确认已有备份。

当前已安全处理内容冲突，但跨设备“删除”的完整墓碑协议仍在后续待办中。在此之前，重要批量删除完成后应立刻在另一台设备同步并检查结果。

## 五、大文件上传

- 桌面端默认同步不超过 200MB 的单文件。
- 移动端默认保持 50MB，以降低 Obsidian 被系统回收或内存不足的概率。
- 服务端默认单文件上限为 200MB，可由运营配置调整；插件会取“用户设置”和“服务端上限”中更小的值。
- 网络传输按服务端下发的分片大小进行，当前默认 1MB/片。
- 每片失败最多自动重试 3 次；整个任务失败后会清理临时上传会话。
- 设置页和状态栏会显示上传进度。

注意：Obsidian 的 Vault 接口仍需要插件先把单个文件读入内存，因此“分片传输”降低的是网络重试成本，并不等于磁盘流式读取。手机上不要盲目提高到 200MB。真正的断点续传、跨启动恢复和流式读取仍属于后续增强。

## 六、更新和公告

插件运行且联网时，约每 5 分钟检查一次。你可以手动“检查更新与公告”：

- 新版本同一版本只提醒一次。到 BRAT 中检查更新，完成后确认设置页版本号；必要时重载插件。
- 有未读公告时，Obsidian 右下角显示缓慢闪烁的铃铛；点击即可查看，不会自动弹窗打断阅读。
- 设置顶部显示未读公告。请手动点击“标记已读”，之后不再置顶。
- 设置最底部的“公告栏”保留本设备收到的最近 10 条公告。只打开公告不会自动标记已读。
- 可以关闭公告图标闪烁；系统启用减少动态效果时也不播放动画。
- 已读状态保存在当前设备，不跨设备同步。Obsidian 关闭、断网或被手机系统挂起时无法及时接收，不是系统推送。

也可以从命令面板打开 NoteCloud 公告栏。更新来源是 [GitHub 发布页](https://github.com/lby7469/obsidianplug/releases)；网络暂时访问不了 GitHub 时请稍后重试，不要下载来源不明的安装包。

## 七、早期用户 5GB 兑换码

1. 在插件设置的“额度”区点击“输入兑换码”。
2. 输入运营人员发放的早期用户兑换码并兑换。
3. 成功后账号增加 5GB，后台同时保留“早期用户”身份标签。
4. 日后充值扩容只调整额度，不会自动移除早期用户身份。

兑换码只能使用一次，默认 30 天有效，具体以管理员发放时告知的期限为准。新码较长，请完整复制，不要在群聊、截图或反馈正文展示未使用的码。每账号每分钟最多尝试 10 次；24 小时窗口内累计超过 100 次请求会锁定兑换功能（包括无效格式和受限请求），请通过意见反馈联系管理员解除。笔记访问和同步不因此停用。请勿尝试猜码。

## 八、多端使用建议

官方 Obsidian 当前支持 Windows、macOS、Linux、Android、iOS/iPadOS，本插件清单已设置为非桌面专用，并基于 Obsidian Vault、MetadataCache 和网络 API 实现，目标是在这些官方平台使用同一套同步协议。

鸿蒙需要区分：

- 能兼容并运行 Android 版 Obsidian 的设备：可作为实验性兼容测试；
- 纯 HarmonyOS NEXT：Obsidian 没有官方原生客户端时，NoteCloud 插件不能单独运行，因此不能承诺支持。

首次增加新设备时，先手动同步一次；不要在多台设备离线修改同一文件后立即批量同步。不要让 NoteCloud 与另一套双向同步服务同时管理同一 Vault，除非已经明确测试过冲突行为。

## 九、遇到问题

请记录以下信息后反馈：

- Obsidian 版本、NoteCloud 插件版本；
- 操作系统与设备型号；
- 出问题的文件类型和大小；
- 设置的冲突策略与同步范围；
- 错误提示、发生时间；
- 是否同时启用了其他同步工具。

不要发送账号密码、完整设备令牌或包含隐私的笔记正文。

## 十、分享给指定的人，或投稿到首页

1. 在网页“我的云仓库”左侧选择文件，在右侧菜单点击“分享文件”。
2. 默认“仅链接访问”：选择 1、7、30 或 365 天有效期，生成并复制完整链接，私下发给对方。
3. 拿到完整链接的人无需登录即可查看分享副本；链接可能被转发，因此这不是按收件人身份授权。不要把链接发到公开群或搜索引擎。
4. 在“我的分享与审核”可以重新生成链接或撤销分享。重生成会使旧链接失效，并默认续期 7 天；撤销不会删除原笔记，但无法收回别人已经下载的文件。
5. 希望出现在首页时，选择“公开到首页”，由管理员审核后展示。目前首页投稿支持 Markdown。

仅链接分享不会进入首页、搜索推荐或后台审核列表，只开放单个文件的当前快照，不会自动开放链接指向的其他笔记、附件或仓库。Markdown 可以预览，其他文件下载后打开；链接预览不会自动加载嵌入图片等资源。需要一起发送的附件请另外生成分享链接。修改原笔记后，如需更新分享内容，请重新分享，旧链接会失效。

同一个文件可以同时存在首页投稿和链接分享。创建链接分享不会取消已经存在的公开投稿，也不会修改“仓库公开权限”；若希望收回所有公开访问，请同时检查这些设置。公开仓库的内容不等于已经过首页审核。

## 十一、意见反馈与容量奖励

插件设置或命令面板打开“意见反馈”，也可以使用网站导航中的“意见反馈”。填写标题和问题描述后提交；系统只提交你填写的内容和来源，不自动附带笔记或日志。

在网站“意见反馈”查看管理员回复、处理状态和奖励结果。管理员采纳后可以赠送容量，同一条反馈只奖励一次，奖励数量以回复及到账结果为准，并非每条反馈都有奖励。每账号每天最多提交 10 条，短时间内相同内容会合并。

## 十二、安全和隐私：我们做了什么，没有做什么

- 正式域名使用 HTTPS 保护网络传输；插件通过网页一次性授权连接，设备令牌可轮换、撤销，不保存账户密码。
- 密码使用随机盐和 scrypt 哈希保存，不是把密码明文存入数据库。密码哈希与笔记加密是两件不同的事。
- 默认私人仓库通过账户权限隔离。管理后台和管理员接口不提供私人笔记目录、正文预览或下载，也不能代设现有用户密码以进入其账户。
- 管理员可以看到账户联系方式、容量、状态、用户主动提交的反馈，以及提交到首页供审核的分享副本。
- 仅链接分享使用随机访问凭证，知道文件编号本身不足以访问。请把整个链接当作钥匙保管；管理员若从你处收到完整链接，也能像普通收件人一样访问。
- 当前不是端到端加密，也未实现笔记内容的应用层静态加密。笔记与备份保存在服务器，拥有服务器、数据库或备份权限的运维人员仍可能读取。不能宣称“平台绝对无法查看”。
- 本地仓库、插件配置里的设备令牌、已下载的文件仍需要你妥善保管。不要把整个插件配置文件发给别人。

特别敏感的资料，在你接受上述边界之前请不要上传。云同步不能替代独立备份；升级、多端首次连接和批量改动前建议额外备份。

## 十三、为什么使用 NoteCloud

- 稳定：保留文件夹层级，提供冲突副本、版本检查、分片重试等保护机制；仍处于早期体验阶段，不承诺零故障或全天候 SLA。
- 实惠：可以通过运营活动获得早期用户免费 5GB；后续普通扩容可叠加，不会自动抹去早期身份。支付与自动充值尚未开放，价格以正式公告为准。
- 安全：HTTPS、密码加盐哈希、设备授权、兑换码防猜测和管理操作记录。
- 隐私：默认私有，公开投稿与仅链接分享分开，后台关闭私人笔记访问，并公开说明加密边界。

平台兼容基于 Obsidian API 设计，不能代替 Android、iOS、macOS 等真实设备逐一测试。首次体验请先用小型测试仓库确认，重要数据保留独立备份。
